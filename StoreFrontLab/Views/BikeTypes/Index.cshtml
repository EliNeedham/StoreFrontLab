@model IEnumerable<StoreFrontLab.DATA.EF.BikeType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BikeType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BikeType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BikeTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BikeTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BikeTypeID })
        </td>
    </tr>
}

</table>
